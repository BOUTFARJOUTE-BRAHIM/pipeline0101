trigger:
-  main


variables:
  brahim:  'helloyeeessiiiiir'



stages:

- stage: build

  jobs:
    - job: buildi_baabaha
      container:
          image: nginx
      variables:
          hhh:  "hohoh"
      steps:
        - script: |
                echo  $(brahim)
                ls -l /
                echo $(hhh)

 
    - job: buildi_babaha2
      variables:
        ZZZ: "ettarch"
      container:
          image: ubuntu-latest
      steps:
        - script: |
                echo "hello"